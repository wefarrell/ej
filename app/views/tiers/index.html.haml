%h1 Listing tiers

%table
  %tr
    %th Name
    %th Cost
    %th
    %th
    %th

  - @tiers.each do |tier|
    %tr
      %td= tier.name
      %td= tier.cost
      %td= link_to 'Show', tier
      %td= link_to 'Edit', edit_tier_path(tier)
      %td= link_to 'Destroy', tier, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Tier', new_tier_path
